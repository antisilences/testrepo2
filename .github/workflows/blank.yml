name: Diff Check
on: push

jobs:
 compare:
   runs-on: ubuntu-latest
   steps:
     - uses: actions/checkout@v2
     - name: Compare Files
       uses: rioj7/action-diff-files@v1
       with:
         files: users_1.yml,users_2.yml
